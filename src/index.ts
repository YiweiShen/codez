/**
 * @file Entry point for the GitHub Action runner.
 * This module imports and invokes the main orchestration logic.
 */

import { run } from './main.js';

// Invoke the main execution workflow
run();
